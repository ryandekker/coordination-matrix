# Production environment variables
# NEXT_PUBLIC_API_URL is intentionally NOT set here
# This makes the frontend use relative '/api' paths, which works
# because the frontend and backend share the same domain in production
